#!/bin/sh
#!/bin/bash

chown owner:group object

sh script.sh = (prbl) runs script in new shell
source script.sh = runs script in current shell

echo Hello     World         	==> "Hello World"
echo "Hello     World"    	==> "Hello     World"

dos2unix file 	==> removes \r at the end of the file to make it as in unix

\v = vertical tab
\b = backspace
\a = alarm

$a = value at memory a

printenv = print all environment vars
export = export a variable to be visible to all (children)(prbl) shells
set = list the shell variables
unset a = unset variable a

